apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: bakery
  labels:
    app: payment-service
    app.kubernetes.io/part-of: crumb-and-ember
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        # Applies every file in db/migrations (mounted via the
        # bakery-db-migrations ConfigMap) in order, idempotently, before the
        # main container starts. This is what actually fixes the
        # "relation notification_jobs does not exist" class of failure:
        # previously each service self-migrated its own tables exactly once
        # at boot with no retry, and the readiness probe only checked DB
        # *connectivity* (SELECT 1), not schema presence — so a pod that
        # lost the race with Postgres coming up would pass readiness and
        # then spin forever on queries against tables that were never
        # created. An initContainer blocks pod readiness until every
        # migration has actually succeeded, and is retried by kubelet on
        # failure (RestartPolicy applies to initContainers too).
        - name: db-migrate
          image: postgres:16-alpine
          envFrom:
            - secretRef:
                name: bakery-db-secret
          env:
            - name: HOME
              value: /tmp
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: db-migrations
              mountPath: /migrations
              readOnly: true
            - name: tmp
              mountPath: /tmp
          command:
            - sh
            - -c
            - |
              set -eu
              echo "waiting for postgres..."
              until pg_isready -h postgres -p 5432 -U "$POSTGRES_USER" -d "$POSTGRES_DB" >/dev/null 2>&1; do
                sleep 2
              done
              echo "applying db/migrations..."
              for f in $(ls /migrations/*.sql | sort); do
                echo "  -> $(basename "$f")"
                psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f "$f"
              done
              echo "migrations applied."
      containers:
        - name: payment-service
          image: ghcr.io/singhgauravlinux-coder/bakery-multistage-smtp/payment-service:2e71aee
          imagePullPolicy: Always
          ports:
            - containerPort: 3008
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: SERVICE_NAME
              value: "payment-service"
            - name: PORT
              value: "3008"
            - name: LOG_LEVEL
              value: "info"
            # Transactions are persisted to Postgres (payments table).
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: bakery-db-secret
                  key: DATABASE_URL
            # Razorpay credentials (create the secret from k8s/secrets.example.yaml).
            # Marked optional so the service still boots in mock mode without it.
            - name: RAZORPAY_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: razorpay-credentials
                  key: keyId
                  optional: true
            - name: RAZORPAY_KEY_SECRET
              valueFrom:
                secretKeyRef:
                  name: razorpay-credentials
                  key: keySecret
                  optional: true
            - name: RAZORPAY_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: razorpay-credentials
                  key: webhookSecret
                  optional: true
          readinessProbe:
            httpGet:
              path: /ready
              port: 3008
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3008
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
      volumes:
        - name: db-migrations
          configMap:
            name: bakery-db-migrations
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: bakery
  labels:
    app: payment-service
spec:
  selector:
    app: payment-service
  ports:
    - port: 3008
      targetPort: 3008
      protocol: TCP
